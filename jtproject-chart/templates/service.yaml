apiVersion: v1
kind: Service
metadata:
  name: {{ include "jtproject-chart.fullname" . }}
  labels:
    {{- include "jtproject-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jtproject-chart.selectorLabels" . | nindent 4 }}
